import{useEffect,useState}from"react";import{useLocation}from"react-router-dom";import{FaArrowUp}from"react-icons/fa";
export default function ScrollProgress(){const{pathname}=useLocation();const[percent,setPercent]=useState(0),[visible,setVisible]=useState(false);useEffect(()=>{let frame=0;const update=()=>{if(frame)return;frame=requestAnimationFrame(()=>{const top=window.scrollY,max=document.documentElement.scrollHeight-window.innerHeight;setPercent(max>0?top/max*100:0);setVisible(top>360);frame=0})};update();addEventListener("scroll",update,{passive:true});addEventListener("resize",update);return()=>{removeEventListener("scroll",update);removeEventListener("resize",update);cancelAnimationFrame(frame)}},[pathname]);return <><div className="scroll-progress" style={{transform:`scaleX(${percent/100})`}} aria-hidden="true"/>{visible&&<button type="button" className="back-to-top" onClick={()=>scrollTo({top:0,behavior:"smooth"})} aria-label="Back to top"><FaArrowUp/></button>}</>}