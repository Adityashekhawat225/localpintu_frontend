import { useEffect } from "react";import { useLocation } from "react-router-dom";const SELECTOR="[data-reveal]";
export default function useScrollReveal(){const{pathname}=useLocation();useEffect(()=>{const reduced=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const reveal=(root=document)=>{const nodes=root.matches?.(SELECTOR)?[root]:[...(root.querySelectorAll?.(SELECTOR)||[])];nodes.forEach((node)=>{if(reduced||!("IntersectionObserver"in window))node.classList.add("is-revealed");else if(!node.classList.contains("is-revealed"))observer.observe(node)})};const observer=new IntersectionObserver((entries)=>entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add("is-revealed");observer.unobserve(entry.target)}}),{rootMargin:"0px 0px -7% 0px",threshold:.08});reveal();const mutation=new MutationObserver((items)=>items.forEach((item)=>item.addedNodes.forEach((node)=>{if(node.nodeType===Node.ELEMENT_NODE)reveal(node)})));const root=document.getElementById("root");if(root)mutation.observe(root,{childList:true,subtree:true});return()=>{observer.disconnect();mutation.disconnect()}},[pathname])}