/* eslint-disable react-refresh/only-export-components */
import{createElement,useEffect,useState}from"react";const motionOnly=new Set(["initial","animate","exit","transition","whileHover","whileTap","whileInView","viewport","layout","layoutId","variants","drag","dragConstraints"]);const cache=new Map();const component=(tag)=>{if(cache.has(tag))return cache.get(tag);function MotionLite({children,...props}){const safe={};for(const[key,value]of Object.entries(props))if(!motionOnly.has(key))safe[key]=value;return createElement(tag,safe,children)}cache.set(tag,MotionLite);return MotionLite};export const motion=new Proxy({},{get:(_,tag)=>component(tag)});export function AnimatePresence({children}){return children??null}export function useReducedMotion(){const[get,set]=useState(()=>typeof matchMedia!=="undefined"&&matchMedia("(prefers-reduced-motion: reduce)").matches);useEffect(()=>{const media=matchMedia("(prefers-reduced-motion: reduce)");const update=()=>set(media.matches);media.addEventListener?.("change",update);return()=>media.removeEventListener?.("change",update)},[]);return get}